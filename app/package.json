{
  "name": "ceske-realie-kviz",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^13.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@vercel/analytics": "^1.3.1",
    "dompurify": "^3.4.12",
    "fs-extra": "^11.2.0",
    "lucide-react": "^0.473.0",
    "nanoid": "^5.0.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1",
    "snarkdown": "^2.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "//overrides": "react-scripts 5.0.1 is unmaintained and pins vulnerable build-time transitives. These force patched versions. All are build-only deps, never shipped to the browser. webpack-dev-server is deliberately NOT overridden: v5 is incompatible with react-scripts' dev server config, and its advisories only affect the local dev server.",
  "overrides": {
    "nth-check": "^2.1.1",
    "resolve-url-loader": {
      "postcss": "^8.4.31"
    },
    "@svgr/plugin-svgo": {
      "svgo": "^2.8.3"
    },
    "underscore": "^1.13.8",
    "serialize-javascript": "^7.0.7"
  }
}
